{
  "name": "json-ld-converter",
  "version": "1.0.0",
  "description": "a script that converts xml/kml to jsonld",
  "main": "index.js",
  "scripts": {
    "test": "jest ${1}",
    "test-watch": "jest ${1} --watch",
    "coveralls": "jest --coverage && cat ./coverage/lcov.info | coveralls",
    "lint": "tslint index.ts lib/**/*.ts test/**/*.ts --exclude '**/*.d.ts'",
    "build": "tsc",
    "validate": "npm ls",
    "prepare": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^3.1.2"
  },
  "dependencies": {
    "@types/node": "^10.11.6",
    "isomorphic-fetch": "^2.2.1",
    "jsonld": "^1.1.0",
    "node-fetch": "^2.2.0",
    "read-xml": "^3.0.0",
    "sparql": "^0.1.3",
    "sparql-client-2": "^0.6.3",
    "sparql-http-client": "^1.0.2",
    "stream": "0.0.2",
    "xml2js": "^0.4.19"
  }
}
